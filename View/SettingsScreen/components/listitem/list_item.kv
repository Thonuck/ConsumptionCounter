<BaseListItemWithSwitch>
    divisor: None
    _no_ripple_effect: True
    on_size: self.ids._right_container.height = dp(64)

    RightSwitchContainer:
        # icon_active: "check"
        # thumb_color_active: "white"
        # thumb_color_inactive: "#60675B" 
        # track_color_active: "darkgreen"
        # track_color_inactive: "#07DECD"

<BaseListItemWithLabel>
    divisor: None
    on_size: self.ids._right_container.height = dp(64)

    RightLabelContainer:
        text: "17:00"
        # icon_active: "check"
        # thumb_color_active: "white"
        # thumb_color_inactive: "#60675B" 
        # track_color_active: "darkgreen"
        # track_color_inactive: "#07DECD"

<BaseListItemWithButton>
    divisor: None
    on_size: self.ids._right_container.height = dp(64)
    current_time: time_button.text

    RightButtonContainer:
        id: time_button
        on_release: self.on_button_pressed()
        size_hint: 1, 1
        text: root.current_time
